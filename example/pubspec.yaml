name: flutter_local_notifications_ohos_example
description: Demonstrates how to use the flutter_local_notifications_ohos plugin.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications_ohos:
    path: ../
  timezone: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
